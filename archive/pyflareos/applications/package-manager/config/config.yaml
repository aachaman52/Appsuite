# Package Manager — default configuration
# dev.pyflare.PackageManager

app:
  id: "dev.pyflare.PackageManager"
  name: "Package Manager"
  version: "1.0.0"

ui:
  theme: system
  language: system
  scale: 1.0

general:
  autostart: false
  minimize_to_tray: false
  check_updates: true
