# PyFlare Store — default configuration
# dev.pyflare.Store

app:
  id: "dev.pyflare.Store"
  name: "PyFlare Store"
  version: "1.0.0"

ui:
  theme: system
  language: system
  scale: 1.0

general:
  autostart: false
  minimize_to_tray: false
  check_updates: true
