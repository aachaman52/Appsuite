# AppSuite — default configuration
# dev.pyflare.AppSuite

app:
  id: "dev.pyflare.AppSuite"
  name: "AppSuite"
  version: "1.0.0"

ui:
  theme: system
  language: system
  scale: 1.0

general:
  autostart: false
  minimize_to_tray: false
  check_updates: true
