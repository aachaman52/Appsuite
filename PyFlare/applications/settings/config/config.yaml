# PyFlare Settings — default configuration
# dev.pyflare.Settings

app:
  id: "dev.pyflare.Settings"
  name: "PyFlare Settings"
  version: "1.0.0"

ui:
  theme: system
  language: system
  scale: 1.0

general:
  autostart: false
  minimize_to_tray: false
  check_updates: true
